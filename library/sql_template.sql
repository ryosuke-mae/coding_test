SELECT
    カテゴリー列, 
    集計関数(集計したい列)  -- SUM, AVG, COUNT, MAX, MIN
FROM
    テーブル名
WHERE
    集計「前」の絞り込み条件  -- (例: 2025年のデータだけ、など)
GROUP BY
    カテゴリー列             -- (例: 商品カテゴリごとにまとめる)
HAVING
    集計「後」の絞り込み条件  -- (例: 合計が1万円以上、など)
ORDER BY
    並び替えたい列;